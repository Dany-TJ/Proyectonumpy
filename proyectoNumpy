import numpy as np

def calcular_promedio_edad(edades):
    """
    Calcula el promedio de edades.

    Parameters:
    - edades: Un arreglo de NumPy que contiene las edades.

    Returns:
    - El promedio de edades.
    """
    return np.mean(edades)

def main():
    # Suponiendo que 'data' es tu DataFrame con una columna 'age'
    edades = np.array(data['age'])

    # Calcular el promedio de edad
    promedio_edad = calcular_promedio_edad(edades)

    # Imprimir el resultado
    print(f"El promedio de edad de las personas participantes en el estudio es {promedio_edad}.")

if __name__ == "__main__":
    # Aquí puedes cargar tu DataFrame 'data' y luego llamar a la función main()
    # Ejemplo: data = cargar_datos()
    main()
